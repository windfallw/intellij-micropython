from typing import List


def run(data: bytearray, points: int, shift: int = 0) -> List:
    ...


def freq(points: int, sample_rate: int) -> List:
    ...


def amplitude(FFT_res: List) -> List:
    ...
